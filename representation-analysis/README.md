# Compatibility Directory

The active project layout has been promoted to the repository root. This directory is kept only so older commands such as `representation-analysis/lm-evaluation-harness/...` continue to resolve during the transition.

Use root-level paths for new work:

- `lm-evaluation-harness/`
- `drawing/`
- `training/`
- `compression/`
- `analysis/`
