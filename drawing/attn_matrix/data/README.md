# Data

The attention-matrix plotting data is stored under `../source/` as saved bundles and manifests. The `server/` scripts are generation code; paper replotting should use the local saved data bundles.
