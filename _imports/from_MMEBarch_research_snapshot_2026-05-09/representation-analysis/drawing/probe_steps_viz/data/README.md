# Probe Step Data

The full hidden-space generation JSON files used by these scripts are large and live on the server under the `ROOT` path configured in each script.

For paper figure reproduction without the large JSON files, use:

- `../embedded_data/paper_Qwen3-4B-Instruct-2507_hidden_both_block_para_perp_sampled_layers.py`
- `../embedded_data/paper_Qwen3-30B-A3B_hidden_both_block_para_perp_sampled_layers.py`

Those embedded scripts contain the exact plotted layer/step mean/min/max arrays.
